import 'package:vast_world/vast_world_share.dart';

typedef ClientWidgetT = ({
  /// In the real Flutter App we are using [Widget].
  String content,
  // left this place for meta data
});

class TestWidgetRenderConfigure extends RenderConfigure {
  const TestWidgetRenderConfigure();
}
