syntax = "proto3";

package vw.share;

// Enum describing server answer on the call a service.
enum ServerAnswerTypeEnumBase {
  UNSPECIFIED_SERVER_ANSWER_TYPE = 0;

  ACCEPTED_SERVER_ANSWER_TYPE = 200;
  REJECTED_SERVER_ANSWER_TYPE = 400;
}
