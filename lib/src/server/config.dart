part of '../../vast_world_server.dart';

typedef C = Config;

class Config {
  static final names = Names.agis();
  static const type = ServerType(name: 'Aide Builder', symbol: '🧠');

  static const showLogBloc = true;
}
