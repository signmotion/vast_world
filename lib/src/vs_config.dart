part of '../vast_world.dart';

class VSConfig {
  /// Only considers this number of bytes when converting an image to JSON.
  /// Set to `-1` if need an all data.
  static const shrinkImageDataInComponent = -1;
}
